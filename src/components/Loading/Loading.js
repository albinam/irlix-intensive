import React from "react";
import './Loading.scss';

function Loader() {
    return (
        <div className="loader__container">
            <div className="loader"/>
        </div>
    );
}

export default Loader;